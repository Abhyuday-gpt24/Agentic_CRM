import { AuthCard } from "./(frontend)/components/auth/auth_card";

export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-50 p-4  from-slate-100 via-slate-50 to-slate-100">
      <AuthCard />
    </div>
  );
}
